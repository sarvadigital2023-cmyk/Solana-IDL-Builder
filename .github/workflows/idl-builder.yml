name: Generate Anchor IDL
on:
  workflow_dispatch:
    inputs:
      job_id:
        description: Job ID for tracking
        required: true
        type: string

env:
  SOLANA_VERSION: "1.18.26"
  SOLANA_INSTALL_DIR: /home/runner/.local/share/solana/install
  SOLANA_BIN: /home/runner/.local/share/solana/install/active_release/bin

jobs:
  generate-idl:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Verify lib.rs
        run: |
          FILE="inputs/${{ inputs.job_id }}/lib.rs"
          if [ ! -f "$FILE" ]; then
            echo "ERROR: lib.rs not found at $FILE"
            exit 1
          fi
          echo "lib.rs found: $(wc -c < $FILE) bytes"
          cp "$FILE" /tmp/lib.rs

      - name: Install system deps
        run: |
          sudo apt-get update -qq
          sudo apt-get install -y pkg-config build-essential libudev-dev libssl-dev curl bzip2

      - name: Setup Rust stable
        uses: dtolnay/rust-toolchain@stable

      - name: Cache Cargo registry
        uses: actions/cache@v4
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
          key: cargo-registry-anchor0301-stable-${{ runner.os }}-v2

      - name: Cache Anchor CLI binary
        id: cache-anchor
        uses: actions/cache@v4
        with:
          path: ~/.cargo/bin/anchor
          key: anchor-bin-0.30.1-stable-${{ runner.os }}-v4

      - name: Install Anchor CLI 0.30.1
        if: steps.cache-anchor.outputs.cache-hit != 'true'
        run: |
          echo "Rust version: $(rustc --version)"
          cargo install anchor-cli --version "=0.30.1" --force
          anchor --version

      - name: Verify Anchor version
        run: anchor --version

      - name: Cache Solana installation
        id: cache-solana
        uses: actions/cache@v4
        with:
          path: ${{ env.SOLANA_INSTALL_DIR }}
          key: solana-${{ env.SOLANA_VERSION }}-${{ runner.os }}-v1

      - name: Install Solana ${{ env.SOLANA_VERSION }}
        if: steps.cache-solana.outputs.cache-hit != 'true'
        run: |
          TARBALL="solana-release-x86_64-unknown-linux-gnu.tar.bz2"
          mkdir -p "${{ env.SOLANA_INSTALL_DIR }}"
          curl -L --retry 5 --retry-delay 10 --retry-connrefused \
            "https://github.com/solana-labs/solana/releases/download/v${{ env.SOLANA_VERSION }}/${TARBALL}" \
            -o /tmp/solana.tar.bz2
          tar xjf /tmp/solana.tar.bz2 -C "${{ env.SOLANA_INSTALL_DIR }}"
          ln -sfn "${{ env.SOLANA_INSTALL_DIR }}/solana-release" "${{ env.SOLANA_INSTALL_DIR }}/active_release"

      - name: Add Solana to PATH
        run: |
          echo "${{ env.SOLANA_BIN }}" >> "$GITHUB_PATH"
          "${{ env.SOLANA_BIN }}/solana" --version

      - name: Solana keygen
        run: |
          mkdir -p ~/.config/solana
          solana-keygen new --no-bip39-passphrase -o ~/.config/solana/id.json --force

      - name: Setup Anchor project from template
        run: |
          export PATH="${{ env.SOLANA_BIN }}:$PATH"

          PROG=$(grep -oP 'pub mod \K\w+' /tmp/lib.rs | head -1 || echo deployer)
          echo "Program name: $PROG"

          RAW_ADDR=$(grep -oP '(?<=declare_id!\(")[^"]+' /tmp/lib.rs 2>/dev/null | head -1 || true)
          echo "Raw address: $RAW_ADDR"

          if python3 scripts/check_b58.py "$RAW_ADDR" 2>&1; then
            ADDR="$RAW_ADDR"
          else
            echo "Invalid Base58 — generating fresh keypair..."
            TMP_KEY=/tmp/idl-build-key.json
            solana-keygen new --no-bip39-passphrase -o "$TMP_KEY" --force
            ADDR=$(solana-keygen pubkey "$TMP_KEY")
            echo "Generated: $ADDR"
          fi
          echo "Using address: $ADDR"

          mkdir -p anchor-project/programs/deployer/src
          cp /tmp/lib.rs anchor-project/programs/deployer/src/lib.rs
          cp template/Cargo.toml anchor-project/Cargo.toml
          sed "s/{{PROG_NAME}}/$PROG/g; s/{{PROG_ADDR}}/$ADDR/g" \
            template/programs/deployer/Cargo.toml.tpl > anchor-project/programs/deployer/Cargo.toml
          sed "s/{{PROG_NAME}}/$PROG/g; s/{{PROG_ADDR}}/$ADDR/g" \
            template/Anchor.toml.tpl > anchor-project/Anchor.toml

          if [ "$ADDR" != "$RAW_ADDR" ]; then
            python3 scripts/patch_declare_id.py anchor-project/programs/deployer/src/lib.rs "$ADDR"
          fi

          echo "=== declare_id ===" && grep "declare_id" anchor-project/programs/deployer/src/lib.rs

          # Add .cargo/config.toml to configure build
          mkdir -p anchor-project/.cargo
          cat > anchor-project/.cargo/config.toml << 'CARGO_CONFIG'
[build]
# Use all CPU cores for faster compilation
jobs = 4
CARGO_CONFIG

          # Store program name for later steps
          echo "PROG=$PROG" >> $GITHUB_ENV
          echo "PROG_ADDR=$ADDR" >> $GITHUB_ENV

      - name: Build IDL via anchor-lang idl-build feature
        working-directory: anchor-project
        run: |
          export PATH="${{ env.SOLANA_BIN }}:$PATH"
          PROG="${{ env.PROG }}"
          echo "Building IDL for program: $PROG"
          
          # Delete stale Cargo.lock if any
          rm -f Cargo.lock
          
          # Create output directory for IDL
          mkdir -p target/idl
          IDL_OUT="$(pwd)/target/idl/${PROG}.json"
          
          echo "IDL output will be at: $IDL_OUT"
          
          # Set anchor env var so proc macro writes IDL to our desired path
          # anchor-lang idl-build feature reads ANCHOR_IDL_BUILD_PROGRAM_LIB_PATH
          ANCHOR_IDL_BUILD_PROGRAM_LIB_PATH="$IDL_OUT" \
            cargo build --features idl-build 2>&1
          
          BUILD_EXIT=$?
          echo "Build exit code: $BUILD_EXIT"
          
          # Check if IDL was written to the designated path
          if [ -f "$IDL_OUT" ]; then
            echo "IDL found at designated path!"
          else
            echo "IDL not at designated path, searching in build outputs..."
            # Search in cargo's OUT_DIR locations
            find ./target -name "idl.json" 2>/dev/null | head -10
            find ./target/debug/build -name "*.json" 2>/dev/null | grep -v fingerprint | head -20
            # Try finding any anchor-like JSON (has 'instructions' or 'address' key)
            find ./target -name "*.json" 2>/dev/null | grep -v "rustc_info|fingerprint|CACHEDIR|incremental" | while read f; do
              if python3 -c "import json,sys; d=json.load(open('$f')); print('$f') if 'instructions' in d or 'name' in d else None" 2>/dev/null; then
                echo "Candidate IDL: $f"
              fi
            done
          fi
          
          echo "=== All JSON in target (excluding noise) ==="
          find ./target -name "*.json" 2>/dev/null | grep -v "rustc_info|fingerprint|CACHEDIR|incremental" | head -30

      - name: Save IDL output
        run: |
          PROG="${{ env.PROG }}"
          
          # Primary: check the designated output path
          IDL_OUT="anchor-project/target/idl/${PROG}.json"
          
          if [ -f "$IDL_OUT" ]; then
            IDL="$IDL_OUT"
          else
            # Search in build output dirs for idl.json
            IDL=$(find anchor-project/target -name "idl.json" 2>/dev/null | head -1)
          fi
          
          if [ -z "$IDL" ]; then
            # Broader search - find JSON with 'instructions' key (anchor IDL signature)
            for f in $(find anchor-project/target -name "*.json" 2>/dev/null | grep -v "rustc_info|fingerprint|CACHEDIR|incremental"); do
              if python3 -c "import json; d=json.load(open('$f')); exit(0 if 'instructions' in d else 1)" 2>/dev/null; then
                IDL="$f"
                echo "Found anchor IDL (has instructions): $f"
                break
              fi
            done
          fi
          
          if [ -z "$IDL" ]; then
            echo "ERROR: No IDL found"
            echo "All JSON files in target:"
            find anchor-project/target -name "*.json" 2>/dev/null | head -30
            exit 1
          fi
          
          echo "Found IDL: $IDL"
          cat "$IDL"
          mkdir -p outputs
          cp "$IDL" "outputs/${{ inputs.job_id }}.json"

      - name: Commit IDL output
        env:
          JOB_ID: ${{ inputs.job_id }}
        run: |
          git pull --rebase origin main || true
          git add "outputs/$JOB_ID.json"
          git -c user.email="41898282+github-actions[bot]@users.noreply.github.com" \
              -c user.name="github-actions[bot]" \
              commit -m "IDL output for $JOB_ID"
          git push
